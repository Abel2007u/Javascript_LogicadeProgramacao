alert("Módulo de um número com JS");
let N1 = parseInt(prompt("Por favor, insira um valor do seu agrado: "));
let pos= N1 * -1;

if (N1 == 0) {
    alert ("Deve ser um valor diferente de zero, meu Caro.");
} else if(N1>0) {
    alert ("Esse número: "+N1+", já é positivo(+).")
} else {
    alert ("O módulo desse número é: "+pos+".")
}


    //Tem como - dentro do alert aí não se cria o res.
    // 17) Ler as notas da 1a. e 2a. avaliações de um aluno. Calcular a média aritmética simples e escrever
    // uma mensagem que diga se o aluno foi ou não aprovado (considerar que nota igual ou maior que 6 o
    // aluno é aprovado). Escrever também a média calculada. 
